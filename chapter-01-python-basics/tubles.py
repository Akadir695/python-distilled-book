# We use Tuples to create a collection of data structures
ahmed = ("pass", '100')  
address = ("putney high street",)  
# Tuples can not replaced, deleted,append to existing tubles
ahmed[0] = "fail"  # This will raise an error
ahmed.append("new_item")
